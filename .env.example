# Lumina Read Environment Configuration
# Copy this to .env on your VPS and replace with your MongoDB Atlas credentials:

MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/lumina_read?retryWrites=true&w=majority
